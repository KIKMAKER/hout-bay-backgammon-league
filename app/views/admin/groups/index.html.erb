<h2>All Groups</h2>

<table class="custom-table table">
  <thead>

  </thead>
  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td class="custom-table"><%= link_to group.title, admin_group_path(group) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
